/** @type {const} */
const themeColors = {
  primary: { light: '#6B1D2B', dark: '#6B1D2B' },      // Bordeaux profond
  secondary: { light: '#C9A84C', dark: '#C9A84C' },    // Or doré
  background: { light: '#F5F0E8', dark: '#1a1a1a' },   // Blanc cassé / Noir
  surface: { light: '#FFFFFF', dark: '#2C2C2C' },      // Blanc / Gris foncé
  foreground: { light: '#2C2C2C', dark: '#F5F0E8' },   // Gris foncé / Blanc cassé
  muted: { light: '#7A7A7A', dark: '#A0A0A0' },        // Gris moyen
  border: { light: '#E0D5C7', dark: '#3A3A3A' },       // Bordure claire
  success: { light: '#4CAF50', dark: '#66BB6A' },      // Vert
  warning: { light: '#FFC107', dark: '#FFD54F' },      // Ambre
  error: { light: '#F44336', dark: '#EF5350' },        // Rouge
};

module.exports = { themeColors };
